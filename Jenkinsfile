pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
              sh '''#!/bin/bash
                    echo "Build Task3"                    
                   '''
            }
        }
    }
}
